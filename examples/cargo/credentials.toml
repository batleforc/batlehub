# Store this file at ~/.cargo/credentials.toml (never commit it).
# It authenticates cargo publish/yank commands against the batlehub proxy.
[registries.batlehub]
token = "Bearer change-me-user-token"
