[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "my-private-lib"
version = "0.1.0"
requires-python = ">=3.11"
description = "Example private library published via BatleHub"
dependencies = []
