# Redirect all npm installs through the batlehub proxy.
registry=http://localhost:8080/proxy/my-npm/

# Authentication token for the proxy (matches the token in config.toml).
//localhost:8080/proxy/my-npm/:_authToken=change-me-user-token

# Always-auth ensures the token is sent even for public packages.
always-auth=true
