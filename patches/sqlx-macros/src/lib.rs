// Stub proc-macro crate — never compiled (sqlx `macros` feature is disabled).
// Exists only to satisfy Cargo's dependency resolution without pulling in
// sqlx-macros-core → sqlx-mysql → rsa (RUSTSEC-2023-0071).
