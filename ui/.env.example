# Backend base URL — required when running the Vite dev server separately from the API.
# Copy this file to .env and adjust as needed.
VITE_API_BASE_URL=http://localhost:8080

# Documentation site URL.
VITE_DOCS_URL=https://batlehub.batleforc.fr
